/**
 * This is used in the `required-checks` workflow
 *
 * Exporting like this so we can write an automated test to make sure that job names
 * exist in our workflows
 */
module.exports = ['backend lint'];
